# --- Core runtime -----------------------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-multipart>=0.0.9          # multipart/form-data for /api/voice
httpx>=0.27,<1.0                 # async HTTP (Azure Translator REST, real agent)

# --- Local speech-to-text (offline, zero credentials) -----------------------
# Lets the mock provider genuinely transcribe voice input.  Comment out for a
# slim install; the gateway then uses the browser transcript / canned samples.
faster-whisper>=1.0

# --- LLM (Tax Agent chat) ----------------------------------------------------
# The offline mock LLM needs nothing.  Install the Anthropic SDK and set
# ANTHROPIC_API_KEY + LLM_PROVIDER=anthropic to chat with Claude.
anthropic>=0.40

# --- Document extraction for chat attachments --------------------------------
pypdf>=4.0

# --- Tests (kept here for a single-file POC install) ------------------------
pytest>=8.0
pytest-asyncio>=0.23

# --- Optional cloud SDKs (install only when using real providers) -----------
# azure-cognitiveservices-speech>=1.37
# google-cloud-translate>=3.15
# google-cloud-speech>=2.26
# boto3>=1.34
